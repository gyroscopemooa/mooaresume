import { describe, expect, it, vi } from "vitest"; import { quickEvalFixtures } from "@/fixtures/quick-eval-cases"; import type { QuickAnalysisGateway } from "@/server/ai/quick/openai-responses-gateway"; import type { QuickAnalysisOutput } from "@/server/ai/quick/schema"; import { quickEvalCaseToRequest, runQuickLiveEval } from "./quick-live-eval";  const fixture = quickEvalFixtures[0];  const output: QuickAnalysisOutput = {   schemaVersion: "1.0",   readiness: {     score: 70,     label: "보완 권장",     summary: "구체적인 인수인계 행동 확인이 필요합니다.",     reasons: ["8개월 교대근무 경험이 있습니다."],   },   priorities: [{     title: "행동을 구체화하세요.",     description: "인수인계 내용을 확인하세요.",     category: "evidence",     severity: "high",     evidenceQuote: "교대근무",   }],   revision: {     revisedAnswer: "편의점 야간 아르바이트를 8개월 하며 교대근무를 경험했습니다.",     highlightedPhrases: ["교대근무"],     originalAnnotations: [{ phrase: "교대근무", type: "good", comment: "실제 경험이 드러납니다." }],     reasons: [{       reason: "기간과 경험을 보존했습니다.",       evidenceQuote: "8개월",       category: "objective",     }],     verificationNote: "인수인계 내용을 확인해야 합니다.",   },   verificationQuestions: ["인수인계 시 어떤 정보를 전달했나요?"], };  describe("QUICK live eval runner", () => {   it("maps a fixture to a traceable QUICK request", () => {     expect(quickEvalCaseToRequest(fixture)).toMatchObject({       requestId: `live-eval-${fixture.id}`,       product: "QUICK",       writingMode: fixture.writingMode,       writingStyle: fixture.writingStyle,       targetLength: fixture.targetLength,       documents: [{         kind: "cover_letter",         text: fixture.source,       }],     });   });    it("evaluates gateway output and retains execution metadata", async () => {     const gateway: QuickAnalysisGateway = {       analyze: vi.fn().mockResolvedValue({         output,         execution: {           responseId: "resp-eval-1",           model: "test-model",           promptVersion: "quick-1.0",           rubricVersion: "quick-rubric-1.0",           schemaVersion: "1.0",           inputTokens: 100,           outputTokens: 50,           totalTokens: 150,         },       }),     };     const onComplete = vi.fn();      const results = await runQuickLiveEval(gateway, [fixture], onComplete);      expect(results).toEqual([       expect.objectContaining({         caseId: fixture.id,         passed: true,         model: "test-model",         responseId: "resp-eval-1",         totalTokens: 150,       }),     ]);     expect(onComplete).toHaveBeenCalledOnce();   }); });
