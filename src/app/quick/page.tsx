import { QuickInputPage } from "@/components/quick-input-page";

/**
 * QUICK 입력 화면.
 *
 * 화면 본문은 `components/quick-input-page.tsx`로 옮겼습니다 — 하이브리드 앱
 * 셸(`/app`)이 같은 입력을 그대로 쓰기 때문입니다. 웹에서 보이는 것과 동작은
 * 이 이동 전과 같습니다.
 */
export default function QuickPage() {
  return <QuickInputPage/>;
}
