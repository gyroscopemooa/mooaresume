/**
 * 아직 정하지 않은 값들 — 한 곳에 모아 둡니다.
 *
 * 사용자가 "가격은 나중에 다 만들고 정한다"고 했습니다. 그 말은 이 숫자들이
 * 곧 바뀐다는 뜻이고, 바뀔 숫자가 파일 여기저기 흩어져 있으면 하나를 빠뜨립니다.
 * 화면에 찍히는 값, 결제창에 보내는 값, 표에 적히는 값이 전부 여기서 나옵니다.
 *
 * **여기 숫자만 고치면 됩니다.** 실제 청구는 이 값이 아니라 Polar 상품
 * 설정과 함께 결정되므로, 값을 바꾼 뒤에는 Polar 대시보드의 상품도 같이
 * 확인해 주세요.
 *
 * 이미 팔고 있는 상품(QUICK·PRO·FINAL·AI 이력서 제작)의 값은 여기로 옮기지
 * 않았습니다. 그쪽은 이미 정해져 돌고 있는 값이라, 미정값과 같은 서랍에 두면
 * 어느 것이 확정인지 알 수 없게 됩니다.
 */

/** 경력기술서 1건. 자리표시값. */
export const CAREER_DESCRIPTION_BUILD_PRICE_KRW = 7_900;

/** 포트폴리오 설명글 1건. 자리표시값. */
export const PORTFOLIO_BUILD_PRICE_KRW = 7_900;

/**
 * 법률 문서 1건. 자리표시값.
 *
 * 문서 종류마다 품이 다릅니다(내용증명 한 장과 항소이유서는 다른 일입니다).
 * 지금은 한 값으로 두고, 종류별 차등은 실제 결과물을 몇 건 보고 정합니다 —
 * 그 전에 나누면 근거 없는 숫자가 여덟 개 생깁니다.
 */
export const LEGAL_DOCUMENT_BUILD_PRICE_KRW = 9_900;
