/**
 * 아직 정하지 않은 값들 — 한 곳에 모아 둡니다.
 *
 * 사용자가 "가격은 나중에 다 만들고 정한다"고 했습니다. 그 말은 이 숫자들이
 * 곧 바뀐다는 뜻이고, 바뀔 숫자가 파일 여기저기 흩어져 있으면 하나를 빠뜨립니다.
 * 화면에 찍히는 값, 결제창에 보내는 값, 표에 적히는 값이 전부 여기서 나옵니다.
 *
 * **여기 숫자만 고치면 됩니다.** 실제 청구는 이 값이 아니라 Polar 상품
 * 설정과 함께 결정되므로, 값을 바꾼 뒤에는 Polar 대시보드의 상품도 같이
 * 확인해 주세요.
 *
 * 이미 팔고 있는 상품(QUICK·PRO·FINAL·AI 이력서 제작)의 값은 여기로 옮기지
 * 않았습니다. 그쪽은 이미 정해져 돌고 있는 값이라, 미정값과 같은 서랍에 두면
 * 어느 것이 확정인지 알 수 없게 됩니다.
 */

/** 경력기술서 1건. 자리표시값. */
export const CAREER_DESCRIPTION_BUILD_PRICE_KRW = 7_900;

/** 포트폴리오 설명글 1건. 자리표시값. */
export const PORTFOLIO_BUILD_PRICE_KRW = 7_900;

/**
 * 법률 문서 1건. **사용자 결정(2026-09-06): 종류 구분 없이 99,000원 통일.**
 *
 * 자리표시값이 아니라 사용자가 정한 값입니다. 내용증명 한 장과 항소이유서는
 * 품이 다르지만, 종류별로 나누면 손님이 "왜 이건 비싸냐"를 여덟 번 묻게 되고
 * 우리는 그 답을 아직 갖고 있지 않습니다. 한 값으로 두고 실제 판매를 봅니다.
 *
 * 이 값이면 원가는 넉넉히 덮입니다. 자료를 상한까지 채운 최악의 1건이 모델
 * 단가에 따라 수백 원~수천 원 수준입니다. **다만 상한 자체가 지금 문제입니다**
 * — 의료소송처럼 기록이 1,000쪽을 넘는 사건은 지금 구조가 앞부분만 읽습니다
 * (`LEGAL_CASE_MAX_PROMPT_CHARS`). 값이 아니라 그쪽을 먼저 풀어야 합니다.
 */
export const LEGAL_DOCUMENT_BUILD_PRICE_KRW = 99_000;

/**
 * 사건자료 분석 1건. **사용자 결정(2026-09-06): 문서 작성과 별개 상품.**
 *
 * 이것만 성격이 다릅니다. 나머지는 "낼 서면을 써 주는" 일이지만 이쪽은
 * 자료 전체를 읽어 사실관계·쟁점·증거·타임라인을 세우는 일이라, 자료가 많을수록
 * 품이 커지고 그 위에서 다른 문서들이 서게 됩니다. 사용자가 제시한 범위는
 * 99,000~199,000원이고 그 가운데를 잡았습니다.
 *
 * 값을 다시 정하실 때 여기 숫자만 고치면 화면·결제·표가 함께 따라갑니다.
 */
export const LEGAL_CASE_ANALYSIS_PRICE_KRW = 149_000;
