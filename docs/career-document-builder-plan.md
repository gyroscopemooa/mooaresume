# 직무기술서 만들어주기 — 다음 할 일

- 출처: 사용자 대화, 2026-08-31.
- 상태: **결정 전. 코드로 존재하는 것은 없습니다.** 지금 만들지 않기로 했습니다.
- 같이 볼 것: [`pro-candidate-materials-and-experience-bank.md`](./pro-candidate-materials-and-experience-bank.md), [`create-mode-and-pricing-decision.md`](./create-mode-and-pricing-decision.md), [`positioning-and-differentiation.md`](./positioning-and-differentiation.md)

---

## 1. 무엇

> 이력서, 자기소개서, 자격증, 수료증, 해외 경험, 여행, 그 밖에 지금까지 받은 모든 자료를 넣으면 **직무기술서를 만들어 준다.** 그리고 `심리상담사 쪽으로` 같은 방향을 지정할 수 있게 한다.

지금 제품이 하는 일과 방향이 반대입니다.

```
지금:   자소서가 있다  →  고쳐 준다
이것:   자료만 있다    →  없던 문서를 만들어 준다
```

## 2. 왜 이게 맞는 자리인가

**흩어진 자료를 하나로 모으는 일은 사람이 제일 하기 싫어하고, 기계가 제일 잘합니다.** 자격증 사진, 수료증 PDF, 예전 이력서, 여행 기록이 각각 다른 폴더에 있는 상태에서 "경력기술서 한 장으로 정리"는 반나절짜리 일입니다. 자료를 이미 다 받아 두는 제품에서는 한 번 더 읽는 일입니다.

그리고 **이미 재료가 다 있습니다.** 자료 분류(`document-classify`), 참고자료 예산, 근거 인용 규칙(`evidenceSchema`), 사실 검증(`validator`)이 전부 이 기능이 필요로 하는 것들입니다.

## 3. `+@` 방향 지정

`심리상담사 관련 직무기술서로 만들어줘` 처럼 목표를 주면, **같은 자료에서 다른 문서**가 나옵니다.

- 같은 상담 경험이라도 무엇을 앞에 두고 무엇을 줄일지가 달라집니다.
- 이건 사실을 바꾸는 것이 아니라 **순서와 분량을 바꾸는 것**입니다. 그 선이 지켜지는 한 안전합니다.

## 4. 가장 큰 위험 — 여기가 전부입니다

> **정정(2026-08-31).** 이 문서 초안에 "여기는 원문이 없다"고 적었는데 틀렸습니다. 첨삭의 기준을 그대로 가져다 붙인 것입니다. **넣어 주신 자료 뭉치가 곧 원문입니다** — 이력서, 자기소개서, 자격증, 수료증, 학력, 해외 경험까지 전부 근거입니다. 없는 것은 원문이 아니라 **1:1로 맞춰 볼 짝**입니다.

차이는 이렇습니다.

```
첨삭:      이 문장  →  고친 문장          한 줄씩 맞대어 볼 수 있음
직무기술서: 자료 여러 개  →  한 줄           어느 자료에서 왔는지 적어야 알 수 있음
```

그래서 위험은 "근거가 없다"가 아니라 **"근거가 어디였는지 잃어버리기 쉽다"**입니다. 여러 자료를 하나로 합치는 과정에서, 자격증 이름과 다른 회사의 업무가 한 문장 안에 섞여 버리면 아무도 알아채지 못합니다.

**막는 방법은 하나뿐입니다 — 줄마다 출처를 달고 만드는 것.** 그러면 합치는 순간에 어긋난 것이 드러납니다. 이미 `evidenceSchema`가 하는 일과 같습니다.

지켜야 할 규칙(이미 제품에 있는 것들과 같은 규칙입니다):

- 자료에 **적혀 있는 사실만** 씁니다. 자격증 이름에서 업무를 추론하지 않습니다.
- 문장마다 **어느 자료에서 나왔는지** 남깁니다. 근거 없는 문장은 만들지 않습니다.
- 기간·수치는 자료에 있는 그대로. `validator`의 `NEW_NUMBER` 검사가 그대로 적용되어야 합니다.
- 방향 지정(`심리상담사`)이 **없는 경험을 만들어내는 구실이 되면 안 됩니다.** 방향은 배치만 바꿉니다.
- 비어 있는 항목은 채우지 말고 **"자료에서 확인되지 않습니다"**로 남깁니다. 이건 이미 제품의 문장 규칙입니다.

## 4-1. 어떤 양식으로 낼 것인가

**아직 정해지지 않았습니다.** 아래는 사용자가 본인 편의로 만들어 쓰던 문서(2026-08-31 제공)의 뼈대일 뿐, 이것을 표준으로 삼자는 뜻이 아닙니다. 사용자 본인이 "내 맘대로 쓴 것"이라고 밝혔습니다.

**표준은 따로 조사해서 정해야 합니다.** 기업 제출용 경력기술서 양식, 채용 플랫폼(사람인·잡코리아 등)이 쓰는 항목 구성, 공공기관 표준이력서의 경력 기술 항목을 확인한 뒤에 고릅니다. 그 전까지 아래는 **참고 사례 하나**입니다.

```
회사명                          2024.11 ~ 2025.05 · 7개월
소속 · 직무/직급
  - 담당업무
  - 담당업무
  - 성과 (수치가 있으면 수치)
```

- **최신 경력이 위로.** 국내 기업이 읽는 순서입니다.
- **기간은 시작~종료와 재직 개월 수를 함께.** 개월 수를 사람이 세게 만들면 안 됩니다.
- **회사 아래에 소속·직급, 그 아래 불릿.** 문단으로 쓰면 아무도 안 읽습니다.
- 사용자의 원래 문서에는 자격증·학력·해외 경험이 **경력과 떨어져** 있었습니다. 이 기능은 그것들을 **경력 줄의 근거로 끌어와야** 값이 생깁니다 — 자격증이 목록 한 줄로 남는 것과, 그 자격이 어느 업무에서 쓰였는지 붙는 것은 다른 문서입니다.

**"기업에서 실제로 쓰는 양식"이 목표입니다 — 그래서 양식 조사가 이 기능의 첫 작업입니다.** 예쁜 서식이 아니라, 채용 담당자가 훑는 순서대로 놓인 문서라는 뜻입니다. 그래서 출력은 DOCX가 기본이어야 합니다(첨삭이 이미 냅니다). 사용자가 그 파일을 열어 회사 양식에 맞게 손보는 것까지가 실제 사용 흐름입니다.

## 5. 정하지 않은 것

| | 질문 |
|---|---|
| 상품 | 별도 상품인가, PRO에 포함인가, 아니면 무료 미끼인가 |
| 가격 | 만들어 주는 일은 첨삭보다 출력이 깁니다. 원가가 다릅니다 |
| 출력 | DOCX·한글에서 열리는 형식이 필요합니다(첨삭은 이미 DOCX를 냅니다) |
| 재사용 | 만든 직무기술서를 그대로 PRO 첨삭의 입력으로 넘길 수 있어야 자연스럽습니다 |
| **양식** | **무엇을 표준으로 삼을지. 조사 전입니다. 사용자 개인 문서를 기준으로 삼지 않습니다.** |
| 경험은행 | [`pro-candidate-materials-and-experience-bank.md`](./pro-candidate-materials-and-experience-bank.md)와 같은 것인가, 그 앞단인가 |

## 6. 혼합 파일 — 별도 카테고리는 만들지 않기로

같은 날 나온 이야기라 여기에 같이 적습니다.

> 이력서와 직무기술서가 한 파일에 들어 있었습니다. `혼합` 카테고리를 만들까?

**만들지 않는 쪽을 권합니다.** 카테고리는 **그 자료를 어떻게 읽을지**를 정하는 값인데, `혼합`은 아무것도 정해 주지 않습니다. "모르겠다"는 이미 `UNSET`이 하고 있고, 그쪽은 진행을 막아서 사용자가 고르게 합니다.

그리고 **본인 서류끼리 섞인 것은 애초에 문제가 아닙니다.**

```
이력서 + 직무기술서   →  둘 다 본인이 쓴 것  →  둘 다 근거로 씀
                        →  무엇으로 고르든 결과가 같습니다
```

**위험한 혼합은 딱 하나, 채용공고가 섞인 경우입니다.**

- `채용공고`로 고르면 → 본인 경력이 근거에서 통째로 빠집니다(2026-08-31에 실제로 터진 사고).
- `이력서`로 고르면 → 회사가 요구한 자격이 본인이 가진 것처럼 쓰일 수 있습니다. **이쪽이 더 나쁩니다.**

이건 카테고리로 풀 수 없습니다. 한 파일 안에 성격이 다른 두 문서가 있으면 **나누는 것 말고는 답이 없습니다.**

**그래서 만든다면 카테고리가 아니라 감지와 안내입니다.** 한 파일에 공고 표지(`자격요건`·`우대사항`·`모집분야`)와 본인 서류 표지(`경력사항`·`지원동기`)가 **둘 다** 있으면 이렇게 말하면 됩니다.

> 이 파일에 채용공고와 본인 서류가 같이 들어 있는 것 같습니다. 나눠서 올리시면 둘 다 제대로 씁니다.

빈도는 낮지만(0.1% 수준) 터졌을 때 손해가 큰 쪽이라, 감지만 해 두는 값은 있습니다. **지금은 하지 않습니다.**

### 더 싼 신호 하나 — 채용공고는 보통 딱 하나다

사용자가 짚은 것입니다. 지원 한 건에 채용공고는 **한 개**입니다. 두 개 이상이 공고로 분류됐다면 **적어도 하나는 틀렸습니다.**

- 규칙이 단순하고, 틀릴 여지가 거의 없습니다.
- 오늘 사고는 공고가 **하나**여서 이 규칙으로는 안 걸렸겠지만, 본인 서류 여러 개가 공고로 몰리는 흔한 실수는 이걸로 잡힙니다.
- 이미 넣은 "이름과 내용이 다르면 비워 둔다"와 방향이 같습니다. **틀릴 수 있는 자리에서는 고르지 않는다.**

---

## 7. 다음 할 일 목록에서의 자리

지금 급한 것들(브라우저에서 DB 직접 호출 이전, 광고 전환 라벨, FINAL 결제) **뒤**입니다. 런칭에 필요한 것이 아니라 제품을 넓히는 것이기 때문입니다.

넣는다면 **경험은행보다 먼저**가 맞습니다. 직무기술서는 결과물이 눈에 보이고 혼자 서지만, 경험은행은 그 위에 쌓는 저장소라 결과물이 먼저 있어야 쓸모가 생깁니다.
